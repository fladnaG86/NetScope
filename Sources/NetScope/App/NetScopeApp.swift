import SwiftUI

@main
struct NetScopeApp: App {
    var body: some Scene {
        WindowGroup {
            Text("NetScope — Loading...")
                .frame(minWidth: 800, minHeight: 500)
        }
    }
}